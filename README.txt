# 6.5-Map

Prerequisite map for MIT 6-5.

Open `index.html` in your browser. Styles are in `styles.css` and JavaScript logic in `script.js`.

The page includes separate sections for Major Requirements (MR) and General Institute Requirements (GIR) with placeholder tiers and nodes for easy expansion.

